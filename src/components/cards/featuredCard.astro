---
import { Image } from "astro:assets";
import type { CollectionEntry } from "astro:content";
import { render } from "astro:content";
import Divider from "@/components/bases/divider.astro";
import { categoriesHandler } from "@/lib/handlers/categories";

type Props = {
  article: CollectionEntry<"articles">;
};

const { article } = Astro.props;

const { remarkPluginFrontmatter } = await render(article);
const category = categoriesHandler.oneCategory(article.data.category.id);
---

<article
  class="group relative isolate flex flex-col gap-4 h-full overflow-hidden bg-[color:var(--bg-surface)] shadow-[var(--shadow-soft)] transition-transform duration-200 hover:-translate-y-0.5 hover:shadow-lg"
>
  <a
    href={`/articles/${article.id}`}
    class="block aspect-[4/3] overflow-hidden bg-[color:var(--bg-subtle)]"
  >
    <Image
      src={article.data.cover}
      alt={article.data.covert_alt ? article.data.covert_alt : article.data.title}
      layout="constrained"
      loading="eager"
      format="avif"
      class="w-full h-full object-cover transition-transform duration-300 group-hover:scale-105"
    />
  </a>
  
  <div class="flex flex-col gap-2 flex-1 px-3 pb-3 sm:px-4 sm:pb-4">
    <div class="flex items-center text-xs text-base-content/70 gap-2">
      <span class="text-[color:var(--primary)] font-medium uppercase tracking-wide">
        {category.data.title}
      </span>
      <Divider />
      <span>{remarkPluginFrontmatter.minutesRead}</span>
    </div>
    
    <h2 class="text-lg md:text-xl font-semibold leading-tight group-hover:text-[color:var(--accent)] transition-colors">
      <a href={`/articles/${article.id}`}>
        <span class="absolute inset-0 z-10 pointer-events-none"></span>
        {article.data.title}
      </a>
    </h2>
    
    <p class="text-sm text-base-content/80 line-clamp-2">
      {article.data.description}
    </p>
  </div>
</article>
